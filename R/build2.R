# An optional custom script to run after Hugo builds your site.
# You can delete it if you do not need it.
# # clean up tex files
# latex_files <- list.files('static', pattern = ".*TD_.*.pdf",
#                           recursive = TRUE, full.names = TRUE)
# file.remove(latex_files)
# # clean up html files
# html_files <- list.files('static', pattern = "*.html",
#                           recursive = TRUE, full.names = TRUE)
# file.remove(html_files)
